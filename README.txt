A simple Cromlech test application that tries to use a minimal subset of the
cromlech components in order to render a view for a content object with
the publisher.

How to install:

$ python bootstrap.py

$ bin/buildout

$ bin/paster serve parts/etc/deploy.ini

Access the application through http://localhost:8080
 
